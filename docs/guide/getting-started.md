# Getting started

AD101 is a compact primer on **signals** — the voltages that move through every analog circuit. You will learn to read three plots an analog engineer lives in: the **waveform** (time), the **spectrum** (frequency), and the **Bode plot** (what a circuit does to a signal).

## Prerequisites

| Requirement | Notes |
|-------------|--------|
| **IC101** completed | Docker + IIC-OSIC-TOOLS noVNC desktop working |
| Shared **workspace** cloned | Same clone you used for IC101 |
| High-school math | Sine, logarithm, basic algebra — no calculus |

If the smoke test from IC101 still passes, you are ready.

## Path through this course

| Step | Guide | Lab |
|------|--------|-----|
| 1 | [Signals in time](guide/signals-in-time.md) | [Lab 01 — Signal explorer](labs/lab-01-signal-explorer-overview.md) |
| 2 | [Building signals](guide/building-signals.md) | [Lab 02 — Harmonic builder](labs/lab-02-harmonic-builder-overview.md) |
| 3 | [The frequency domain](guide/the-frequency-domain.md) | [Lab 03 — Spectrum detective](labs/lab-03-spectrum-detective-overview.md) |
| 4 | [Filters & Bode](guide/filters-and-bode.md) | [Lab 04 — RC filter & Bode](labs/lab-04-rc-bode-overview.md) |

Keep [Reading plots](reference/reading-plots.md) open as a cheat sheet. If a plot window won't open, see [Python in the workbench](reference/python-in-the-workbench.md).

## Clone AD101 into the workbench

From your host machine (next to the `workspace` folder, or inside it under `modules/`):

```bash
cd workspace/modules
git clone https://github.com/uoftasic/ad101.git
```

Inside the noVNC desktop:

```bash
. /foss/designs/common/.designinit
mod ad101
python3 scripts/check_env.py
```

You should see `OK` lines for numpy, matplotlib, and the `adsig` smoke test.

## How labs work

Every lab has an interactive explorer and a headless export mode:

```bash
cd labs/lab-01-signal-explorer
python3 src/explore.py              # drag sliders
python3 src/explore.py --headless   # write PNGs if no GUI
```

The figures on this docs site were generated by the same scripts (`scripts/build_figures.py`), so what you read matches what you run.

## What this course is not

- Not a replacement for ECE signals courses
- Not a deep dive into Fourier proofs or complex numbers
- Not XSchem / ngspice yet — that starts in AD102 / AD103

Next: [Signals in time](guide/signals-in-time.md).
